# PROGRESS — Инфраструктура Pulsar

Расширение всех трёх режимов (Монтаж / VUB / Замена титров) общими продакшен-фичами.

## Фаза 1: GPU-ускорение (NVENC/QSV/AMF + fallback на CPU)
- [x] 1.1 Общий модуль `electron/ipc/encoder.ts`: автодетект аппаратного энкодера (реальный тест-энкод, кэш)
- [x] 1.2 Маппинг x264 preset/crf → nvenc preset/cq; функция `videoEncoderOptions()`
- [x] 1.3 Подключить хелпер во все 4 места кодирования (ffmpegRender база+копии, vub, cleaner)
- [x] 1.4 Настройка GPU (auto/gpu/cpu) в конфиге + IPC `getGpuMode`/`setGpuMode`
- [x] 1.5 UI-переключатель GPU (вкладка «Производительность» VUB — настройка глобальная, действует на все режимы)

## Фаза 2: Пресеты площадок (TikTok/Reels/Shorts/YouTube)
- [x] 2.1 Каталог пресетов `src/data/platformPresets.ts` (формат, разрешение, max-длительность)
- [x] 2.2 Применение пресета: формат+качество+подрезка длительности (Монтаж)
- [x] 2.3 UI выбора пресета в ExportModal (VUB сохраняет разрешение оригинала — пресеты неприменимы)

## Фаза 3: Очередь задач + фоновый рендер
- [x] 3.1 Единый тип Job + общий стор очереди `src/store/queueStore.ts`
- [x] 3.2 Свести vub/cleaner/export прогресс в общую очередь (глобальные слушатели в App)
- [x] 3.3 Экран «Очередь» (статусы, прогресс, отмена всех, очистка) + бейдж в TopBar + карточка на старте

## Фаза 4: История / проекты (персистентность)
- [x] 4.1 Модуль хранилища на диске `electron/ipc/store.ts` (json в userData)
- [x] 4.2 Сохранение завершённых задач в историю (Монтаж/VUB/Cleaner) + IPC
- [x] 4.3 Экран «История» (открыть папку результата, повторить режим, удалить, очистить)

## Фаза 5: Разнообразие монтажа (эффекты по биту + новый функционал)
Рендер приводится в соответствие с live-превью: эффекты срабатывают импульсом в момент
бита, с учётом варианта и силы. Реализованы ранее «превью-only» эффекты + новые приёмы.
- [x] 5.A1 Провести тайминг/вариант/интенсивность эффектов в рендер; импульс по биту (flash/hue/prism/rgb) — `src/data/effectRender.ts` — тест: `tsc --noEmit` + ffmpeg filterchain
- [x] 5.A2 Анимированный zoom-punch (zoompan, варианты in/out/punch) — тест: ffmpeg `-filter_script` exit 0
- [x] 5.A3 Реализовать в рендере Split / Boomerang(approx) / FastCut / Speed (темп с сохранением длительности и синхрона) — тест: ffmpeg прогон каждого графа exit 0
- [ ] 5.B1 Переходы на бит между клипами (whip-pan / zoom / dip-to-black / RGB-split) — тест: рендер 2 клипов
- [ ] 5.B2 Умный выбор кадров по энергии/онсетам — тест: распределение клипов
- [x] 5.B3 Новые эффекты Shake / Glitch / Light Leak (рендер + превью + каталог + типы) — тест: ffmpeg прогон каждого + комбинированный exit 0
- [x] 5.B4 Стиль-пресеты монтажа (Hype / Cinematic / Smooth / Clean) — `src/data/montageStyles.ts` + UI в EditPanel — тест: `tsc --noEmit`

## Доп. (запрос пользователя): навигация + настройки на старте
- [x] N.1 Панель `TopBar` («Назад»/«На главную»/«Выход») на всех экранах вместо незаметной иконки
- [x] N.2 IPC `app:quit` — явный выход из приложения
- [x] N.3 Экран «Настройки» на старте: API-ключ (перенос из VUB), режим GPU, папка по умолчанию
- [x] N.4 История и Очередь — плавающие перетаскиваемые мини-окна (не полноэкранные режимы);
      открываются из TopBar (любой режим) и со стартового экрана; кнопки в TopBar (Очередь+бейдж, История)

## Отдельное приложение «Pulsar Метаданные» под macOS (2026-09-21)

Тот же модуль, своя точка входа: `electron/metadata-main.ts`, `metadata.html`,
`vite.metadata.config.ts`, `electron-builder.metadata.json`. Под Windows —
`npm run meta:dist -- --win --dir`; `.dmg` собирает `.github/workflows/metadata-mac.yml`
(вручную или тегом `meta-v<версия>`). `--smoke <папка>` — проверка собранного приложения.

- [x] точка входа, общий `electron/mediaProtocol.ts` с Pulsar, exiftool на macOS — perl-версия
- [x] в пакете только ffmpeg и exiftool: ~120 МБ ресурсов вместо ~450 МБ
- [x] smoke на Windows-сборке: фото, видео, mp3, m4a, окно, превью — всё OK
- [ ] первая сборка на маке в CI (arm64 + Intel)
- [ ] проверить .dmg на живом маке
- [ ] подпись Developer ID и нотаризация — без них при первом запуске «Всё равно открыть»

## Ревью качества 2026-09-02 (docs/review/)

Консилиум из четырёх ревьюеров упёрся в лимит сессии; уцелели два подотчёта
UX-аудита — `02a_UX_errors.md` (ошибки, падения, хоткеи, обновления) и
`02b_UX_editor.md` (Монтаж, 50 находок). QA, архитектуру и продуктовый разбор
повторить отдельным заходом.

Сделано в тот же день:
- [x] меню Electron снято — Ctrl+R больше не перезагружает окно и не теряет проект; DevTools по F12
- [x] обработчики падений: диалог вместо исчезающего окна, перезагрузка после крэша рендерера
- [x] анализ ритма: подмена равномерной сеткой видна (тост с причиной); прогресс на анализе живой
- [x] ошибки монтажа, Cleaner, экспорта показываются; тосты-ошибки не гаснут, «Подробнее» и «Скопировать»
- [x] отмена экспорта — «Отменено», не ошибка; Ctrl+N реально сбрасывает проект; «На главную» без ложного предупреждения
- [x] баннер обновления показывает ошибку и даёт повторить
- [x] панель инструментов Монтажа и карточки режимов по-русски; ENOENT/EPERM человеческим текстом; :focus-visible

Вторая волна (все четыре отчёта в `docs/review/`):
- [x] реестр дочерних процессов: before-quit и крэш гасят дерево ffmpeg/python/yt-dlp
- [x] отмена рендера по задаче, не глобальным флагом
- [x] рекордер не удаляет произвольную папку; разрешения сессии — только экран и микрофон; shell.openPath — только каталоги
- [x] media:// потоком (падение main на ≥ 2 ГиБ), белый список расширений
- [x] beat_detect.py без resampy — ритм снова определяется на 44,1/48 кГц
- [x] drawtext: текст с «%» больше не пустой; VUB: fs.watch с обработчиком ошибок, зеркало по умолчанию выключено
- [x] мастер: остановка установки, ошибка проверки видна
- [x] очередь и история знают все модули; озвучка и дубляж под своими именами
- [x] волна трека в Монтаже настоящая (`media:waveform`), а не формула
- [x] Метаданные: Бишкек, Ош, Джалал-Абад, Узген, Каракол; офлайн-каталог городов мира (GeoNames, 34 тыс.) с поиском по-русски и по-английски, любой город вводится текстом (2026-09-03)

Не сделано, по отчётам:
- [ ] Alt+F4 без подтверждения при кнопке «Выход» с подтверждением — единый `before-quit`
- [ ] Esc не закрывает модалки Монтажа; ловушка фокуса
- [ ] MediaPicker теряет невыбранные файлы при возврате на экран
- [ ] история экспорта пишет выдуманные имена файлов
- [ ] `window.confirm`/`alert` в TopBar и MediaPicker → свои диалоги; эмодзи и глифы в кнопках → SVG
- [ ] media:// и запись в произвольный путь — реестр разрешённых путей (арх. S2/S13); Electron 34 → актуальный
- [ ] AGPL: `@imgly/background-removal` в бандле — коммерческая лицензия или замена модели (продукт 6.1)
- [ ] музыкальная библиотека — 20 болванок по 20 с и волна-формула: настоящие CC0-треки и `media:waveform`
- [ ] «Назад» стирает состояние в 13 модулях; кнопка запуска VUB в последней вкладке; Pro/Воронка — удалить или вернуть
- [ ] перевыпустить токен Telegram-бота из feedback.secret.json
- [ ] «ты»/«вы» перемешаны; капслок вкладок; 100 копий без оценки времени и места
- [ ] Python-скрипты `detect_overlays.py`, `beat_detect.py`, `upscale.py` — английские отладочные строки
